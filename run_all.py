"""Launcher (STUB, Day 2+).

Will boot every agent in a scenario as its own long-lived process/task (each
``Agent.create(...).run()`` on its own Band account/key), wire them into one room, and
send the opening @mention. For now it only documents intent and exits.

Planned usage:  python run_all.py --domain authbridge --scenario mri_lumbar
"""

if __name__ == "__main__":
    raise SystemExit("run_all.py is a Day-2+ stub — no agents wired yet.")
