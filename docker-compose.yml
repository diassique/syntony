# Syntony — deployment composition.
# One service per long-lived agent process, plus the UI/stream service.
# Each agent receives its Band credentials via env_file (.env), never baked into the image.

services: {}
